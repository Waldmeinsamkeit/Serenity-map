# Serenity Local Store

This folder is reserved for local canvas data, such as exported tldraw
document snapshots, AI context exports, and future file-backed workspace data.

The current browser app still persists the live canvas through tldraw's
browser storage. Writing directly into this folder from the browser requires a
local server/API layer.
